# React Native Messenger Clone

## Description

This is a messenger clone built with React Native. It is a work in progress.


## Install NPM packages
  ```sh
   npm install
   ```

### Run the app
   ```sh
   npx react-native run-macos
   ```
